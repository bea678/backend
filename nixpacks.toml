[phases.setup]
nixPkgs = ["...", "nodejs_20"]
aptPkgs = [
  "python3", 
  "ffmpeg", 
  "ca-certificates", 
  "fonts-liberation",
  "..."
]

[phases.build]
cmds = ["npm install"]

[start]
cmd = "npm start"
